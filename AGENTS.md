<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

فروق أوقعت أخطاء فعلية في هذا المشروع:

- `middleware.ts` صار **`proxy.ts`** (انظر `src/proxy.ts`).
- `params` في الصفحات والتخطيطات **Promise** — لا بد من `await`.
- `eslint` ليس مفتاحًا صالحًا في `NextConfig`.

---

# 3MR Workshop ERP — قواعد المشروع

نظام إدارة ورشة سيارات لـ **شركة سليمان أحمد خميس النعماني للتجارة**.
مشروع مستقل تمامًا: لا يشارك كودًا ولا قاعدة بيانات مع 3MR OS أو 3MR CRM
أو منصة قطع الغيار.

## البنية

```
src/
├── app/[locale]/       مسارات فقط — ملفات رفيعة تركّب من modules
│   ├── (auth)/         شاشات المصادقة (بلا جلسة في هذه المرحلة)
│   └── (app)/          هيكل التطبيق: شريط جانبي + علوي + محتوى
├── components/
│   ├── ui/             مكوّنات shadcn — تُدار عبر CLI، لا تُنسخ يدويًا
│   ├── layout/         الشريط الجانبي والعلوي ولوحة الأوامر
│   └── patterns/       أنماط متكرّرة (بطاقة مؤشّر، شارة حالة، حالة فارغة…)
├── modules/<domain>/   منطق كل وحدة ومكوّناتها وبياناتها
├── config/             مصدر الحقيقة للتنقّل والإجراءات السريعة
├── i18n/               التوجيه متعدد اللغات
└── lib/                أدوات + طبقة جاهزية Auth.js
```

## قواعد ملزمة

1. **لا تلمس `src/components/ui/` يدويًا.** التثبيت والتحديث عبر
   `npx shadcn@latest add|diff <component>`. أي تخصيص يكون في `patterns/` أو عبر التوكنز.
2. **لا ألوان صريحة في المكوّنات.** لا `#hex` ولا `red-500`. توكنز `globals.css` فقط.
   الأحمر `#D61F26` لون **تمييز**: زر الفعل الأساسي، الحالة النشطة، مؤشّر واحد. لا أكثر.
3. **خصائص منطقية دائمًا** — `ms/me`, `ps/pe`, `start/end`, `text-start`.
   ممنوع `ml/mr/left/right`. الواجهة تعمل بالعربي والإنجليزي معًا.
4. **الروابط من `@/i18n/navigation`** لا من `next/link` أو `next/navigation`،
   وإلا ضاعت بادئة اللغة.
5. **لا نص مكتوب داخل مكوّن.** كل نص يُضاف إلى `messages/ar.json`
   و`messages/en.json` معًا — لا يُترك مفتاح في ملف دون الآخر.
6. **التنقّل يُعرَّف مرة واحدة** في `src/config/navigation.ts`. الشريط الجانبي
   ومسار التنقّل ولوحة الأوامر كلها تقرأ منه.
7. **ممنوع جلسة أو كوكي مصادقة وهمي.** المصادقة الآن **Supabase Auth + RLS**
   لا Auth.js — انظر `src/lib/auth/README.md`. مفعّلة فقط عند
   `DATA_SOURCE=supabase`؛ في وضع البذرة لا حارس (لا مستخدمين أصلًا).
8. **الأرقام جدولية** — كل رقم في جدول أو مؤشّر يحمل `data-numeric`،
   والمعرّفات واللوحات تحمل `data-ltr`.
9. **لا نُخفي أخطاء TypeScript.** `ignoreBuildErrors` يبقى `false` دائمًا.
10. **لا أسرار في الكود** — متغيّرات بيئة فقط.
11. **كل قراءة بيانات تمرّ عبر `src/server/repositories`** — لا تستورد
    `modules/<domain>/data.ts` من أي صفحة. السجل هو نقطة التبديل الوحيدة
    بين البذرة المحلية وSupabase.
12. **`src/server/` للخادم فقط.** الملف `repositories/index.ts` يستورد
    `server-only`، فأي مكوّن `"use client"` يستورده **يُفشل البناء**. الحسابات
    الصافية التي تحتاجها الواجهة تعيش في `src/lib/services/` لا في `src/server/`.

## التحقّق قبل أي تسليم

```bash
npx tsc --noEmit && npx next build
```

## نطاق المرحلة الحالية

كل وحدات الواجهة (أوامر التشغيل، المواعيد، التقديرات، العملاء، المركبات، المخزون،
المشتريات، الفواتير، المحاسبة، تقفيل اليومية، الموظفون، التسويق، التقارير، الإعدادات)
مبنية بصفحات قراءة كاملة — قائمة + تفاصيل، وروابط متبادلة صحيحة بينها.

**تحديث معتمد (المرحلة ب):** طبقة الوصول للبيانات صارت **موحّدة لكل الوحدات
الثلاث عشرة**، لا للوحدات المحاسبية وحدها:

- `src/server/repositories/index.ts` — السجل، ونقطة التبديل الوحيدة.
- `src/server/repositories/collection-repository.ts` — العقد العام + المحوّل المحلي.
- `src/server/repositories/local/index.ts` — الملف **الوحيد** الذي يعرف أن
  البيانات تأتي من `modules/<domain>/data.ts`.
- `src/lib/services/` — الحسابات الصافية (الفواتير، التقفيل اليومي). **نُقلت من
  `src/server/services/`** لأن مكوّنات `"use client"` تحتاجها، وإبقاؤها تحت
  `src/server/` كان سيسرّب كود الخادم (ولاحقًا مفاتيح Supabase) إلى المتصفح.

`settings` و`dashboard` تبقيان على الاستيراد المباشر — بياناتهما إعدادات لا مجموعات.

لا يزال **بلا اتصال بأي قاعدة بيانات فعلية، بلا أسرار، بلا نشر**. ملفات
`src/server/db/migrations/*.sql` وثائق تصميم لا يشغّلها أي كود.

بيانات الكيان القانوني (`src/lib/legal/company.ts`) مستخرجة حرفيًا من إثبات العنوان
الوطني وشهادة تسجيل ضريبة القيمة المضافة الرسميين — لا تُعدَّل إلا بمستند رسمي جديد.

**فاتورة/ZATCA:** حساب الضريبة وترميز حمل QR (المرحلة الأولى) وقفل الفاتورة بعد
الإصدار كلها **منفَّذة**. توليد XML/UBL، PDF/A-3، الختم الرقمي، والتكامل المباشر مع
منصة فاتورة (المرحلة الثانية) **معلَّقة (Pending) عمدًا** — راجع
`src/lib/zatca/compliance.ts`. المنشأة لم تستلم إشعار الانضمام لمرحلة الربط بعد.

**نماذج الإنشاء (mutations UI) موجودة فعلًا** خلافًا لما كان مكتوبًا هنا سابقًا:
عميل جديد، موعد جديد، فاتورة مسودة، أمر تشغيل جديد — كلها تحفظ في `localStorage`
عبر `modules/<domain>/client-store.ts`. التحقق من الإدخال في `src/lib/validation/forms.ts`
(مخططات Zod تُعاد لاستخدامها في Server Actions لاحقًا).

**تحديث معتمد (المرحلة هـ): النظام صار CRUD كاملًا لا قراءة فقط.**

- **تعديل وحذف فعليان** في كل وحدة قائمة (عملاء، مركبات، مواعيد، فواتير،
  أوامر تشغيل، مخزون) + الوحدات الجديدة. الحذف بتأكيد يسمّي السجل.
- **محرّك واحد لكل الوحدات:** `src/lib/use-local-collection.ts` — يفحص
  الصلاحية، يكتب، يتحقق من نجاح الكتابة، ثم يحدّث الحالة (بهذا الترتيب).
  استخدم `replaceAll` للعمليات التي تمسّ عدة سجلات دفعةً واحدة؛ استدعاء
  `update` في حلقة يجعل كل نداء يكتب فوق سابقه (خلل رُصد حيًا في استلام
  المشتريات) لأن كلها تُغلِق على نفس لقطة الحالة.
- **حذف سجل بذرة يحتاج شاهدة (tombstone)** وإلا عاد عند التحديث التالي.
- **الصلاحيات:** `src/lib/auth/permissions.ts` هو المصدر الوحيد، ويُستدعى
  عبر `useGuard()` **قبل كل mutation** لا عند إخفاء الأزرار فقط. الموردون
  والمردود موردان جديدان في `Resource`. سياسات RLS في
  `0009_auth_profiles_and_rls.sql` **مولَّدة من نفس الملف** — أعد توليدها
  عند تعديل المصفوفة، لا تحرّرها يدويًا.
- **دور المعاينة** (`role-preview.ts`) لوضع البذرة وحده: ليس جلسة، ولا
  يُقرأ على الخادم، ويتجاهله النظام كليًا عند تفعيل المصادقة.

**وحدات جديدة:** الموردون (`/suppliers`)، المردود (`/returns` — إشعارات
دائنة/مدينة داخل مجموعة الفواتير نفسها لأنها مستندات من نفس النوع)،
فاتورة المشتريات (تدخل المخزون فعليًا عند الاستلام، مرة واحدة بحارس
`receivedAt`)، سجلا المحاسبة بالبحث الزمني والتصدير، منشئ التقارير
(كل مصدر مربوط بمورد صلاحية)، استوديو محتوى التسويق.

**«التقديرات» صارت «التسعير»** في الواجهة — المسار `/estimates` والنوع
`Estimate` لم يتغيّرا (تغيير المسار كان سيكسر الروابط المتبادلة).

⚠️ **`opacity-100!` المطلقة ممنوعة على `DialogContent`** — استخدم
`data-[state=open]:opacity-100!`. التجاوز المطلق يعطّل حركة الخروج فيبقى
الحوار ظاهرًا بعد إغلاقه.

⚠️ **الحوارات لا تُغلق بصريًا في متصفح غير مرئي** (`document.visibilityState
=== "hidden"`): حركات CSS لا تعمل فلا يُطلق `animationend`، وRadix ينتظره
لإزالة العنصر. هذا خلل بيئة لا خلل تطبيق — لا تطارده في الكود.

**بلا** اتصال بقاعدة بيانات فعلية، وبلا مصادقة حقيقية، وبلا نشر أو أسرار خارجية.

**المصادقة (المرحلة د) مبنيّة:** حارس في `src/proxy.ts`، جلسة حقيقية في
`src/lib/auth/session.ts`، `signInAction`/`signOutAction`، وسياسات RLS كاملة
في `0009_auth_profiles_and_rls.sql` (مصفوفة الصلاحيات مولَّدة آليًا من
`permissions.ts` — لا تُحرَّر في SQL يدويًا).

⚠️ **غير مُتحقَّق منه على قاعدة بيانات حقيقية.** ولم يُشغَّل أي ترحيل بعد.

⚠️ **لا تنشر في وضع البذرة** (`DATA_SOURCE` فارغ): المصادقة معطَّلة عمدًا
هناك، فالنشر يعني نظامًا مفتوحًا بلا أي حماية.
