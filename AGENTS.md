<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

فروق أوقعت أخطاء فعلية في هذا المشروع:

- `middleware.ts` صار **`proxy.ts`** (انظر `src/proxy.ts`).
- `params` في الصفحات والتخطيطات **Promise** — لا بد من `await`.
- `eslint` ليس مفتاحًا صالحًا في `NextConfig`.

---

# 3MR Workshop ERP — قواعد المشروع

نظام إدارة ورشة سيارات لـ **شركة سليمان أحمد خميس النعماني للتجارة**.
مشروع مستقل تمامًا: لا يشارك كودًا ولا قاعدة بيانات مع 3MR OS أو 3MR CRM
أو منصة قطع الغيار.

## البنية

```
src/
├── app/[locale]/       مسارات فقط — ملفات رفيعة تركّب من modules
│   ├── (auth)/         شاشات المصادقة (بلا جلسة في هذه المرحلة)
│   └── (app)/          هيكل التطبيق: شريط جانبي + علوي + محتوى
├── components/
│   ├── ui/             مكوّنات shadcn — تُدار عبر CLI، لا تُنسخ يدويًا
│   ├── layout/         الشريط الجانبي والعلوي ولوحة الأوامر
│   └── patterns/       أنماط متكرّرة (بطاقة مؤشّر، شارة حالة، حالة فارغة…)
├── modules/<domain>/   منطق كل وحدة ومكوّناتها وبياناتها
├── config/             مصدر الحقيقة للتنقّل والإجراءات السريعة
├── i18n/               التوجيه متعدد اللغات
└── lib/                أدوات + طبقة جاهزية Auth.js
```

## قواعد ملزمة

1. **لا تلمس `src/components/ui/` يدويًا.** التثبيت والتحديث عبر
   `npx shadcn@latest add|diff <component>`. أي تخصيص يكون في `patterns/` أو عبر التوكنز.
2. **لا ألوان صريحة في المكوّنات.** لا `#hex` ولا `red-500`. توكنز `globals.css` فقط.
   الأحمر `#D61F26` لون **تمييز**: زر الفعل الأساسي، الحالة النشطة، مؤشّر واحد. لا أكثر.
3. **خصائص منطقية دائمًا** — `ms/me`, `ps/pe`, `start/end`, `text-start`.
   ممنوع `ml/mr/left/right`. الواجهة تعمل بالعربي والإنجليزي معًا.
4. **الروابط من `@/i18n/navigation`** لا من `next/link` أو `next/navigation`،
   وإلا ضاعت بادئة اللغة.
5. **لا نص مكتوب داخل مكوّن.** كل نص يُضاف إلى `messages/ar.json`
   و`messages/en.json` معًا — لا يُترك مفتاح في ملف دون الآخر.
6. **التنقّل يُعرَّف مرة واحدة** في `src/config/navigation.ts`. الشريط الجانبي
   ومسار التنقّل ولوحة الأوامر كلها تقرأ منه.
7. **ممنوع جلسة أو كوكي مصادقة وهمي** قبل وصول Auth.js. انظر `src/lib/auth/README.md`.
8. **الأرقام جدولية** — كل رقم في جدول أو مؤشّر يحمل `data-numeric`،
   والمعرّفات واللوحات تحمل `data-ltr`.
9. **لا نُخفي أخطاء TypeScript.** `ignoreBuildErrors` يبقى `false` دائمًا.
10. **لا أسرار في الكود** — متغيّرات بيئة فقط.

## التحقّق قبل أي تسليم

```bash
npx tsc --noEmit && npx next build
```

## نطاق المرحلة الحالية

كل وحدات الواجهة (أوامر التشغيل، المواعيد، التقديرات، العملاء، المركبات، المخزون،
المشتريات، الفواتير، المحاسبة، الموظفون، التسويق، التقارير، الإعدادات) مبنية بصفحات
قراءة كاملة — قائمة + تفاصيل، بيانات Mock نمطية، وروابط متبادلة صحيحة بينها.
العمل الحالي هو **الاستقرار والتكامل**: تماسك الروابط، دقة الترجمة وRTL/LTR،
الوصولية والاستجابة على الجوال، وربط لوحة التحكم ببيانات الوحدات الفعلية بدل أرقام منفصلة.
**بلا** إنشاء أو تعديل أو باك-إند أو قاعدة بيانات أو APIs أو مصادقة أو ZATCA.
